# E-Commerce

## Set Up

1. Instalar dependencias: `npm run i`.
2. Configurar la base de datos: completar `.env`.
3. Ejecutar migración: `npx prisma migrate dev`.
4. Poblar la base de datos: `npm run seed`.
5. Iniciar servidor: `npm run dev`.
